# src/types

Definitions et interfaces TypeScript partagees dans tout le projet (types de
domaine : `Lead`, `Company`, `Campaign`, types de configuration, types
d'entree/sortie des services). Centralise les contrats de donnees pour eviter
les duplications entre modules.
